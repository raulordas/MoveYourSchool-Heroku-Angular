import { FilterActividadesPipe } from './filter-actividades.pipe';

describe('FilterActividadesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterActividadesPipe();
    expect(pipe).toBeTruthy();
  });
});
